import { createVuetify } from "vuetify";

const vuetify = createVuetify({
  theme: {
    defaultTheme: "dark", // Use "dark" instead of "dark: true"
  },
});

export default vuetify; // Add default export for vuetify
